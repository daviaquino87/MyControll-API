import { CreateTransactionUseCase } from "./CreateTransactionUseCase";

let createTransactionUseCase: CreateTransactionUseCase;

describe("Transactions tests", () => {
  it("should be able to possible to create a new transaction", () => {});
});
